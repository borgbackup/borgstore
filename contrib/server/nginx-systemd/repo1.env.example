# Per-repo environment file for borgstore@repo1.service
# Copy to /etc/borgstore/repo1.env and set correct values.
# Permissions: chmod 600, owned by root or the borgstore service user.

BORGSTORE_BACKEND=file:///srv/borgstore/repos/borg/repo1
BORGSTORE_USERNAME=myuser
BORGSTORE_PASSWORD=changeme
